# Docker environment variables for development
COMPOSE_PROJECT_NAME=yolo-retrain-dev

# Database passwords
POSTGRES_PASSWORD=airflow123
MLFLOW_DB_PASSWORD=mlflow123

# Airflow configuration
AIRFLOW_SECRET_KEY=dev_secret_key_change_in_production

# MLflow configuration
MLFLOW_TRACKING_URI=http://localhost:5000

# Optional: AWS S3 for artifacts (uncomment if using)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-west-2

# Optional: Email notifications
# AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
# AIRFLOW__SMTP__SMTP_USER=your_email@gmail.com
# AIRFLOW__SMTP__SMTP_PASSWORD=your_app_password

# GPU support (uncomment if using NVIDIA GPU)
# NVIDIA_VISIBLE_DEVICES=all
# NVIDIA_DRIVER_CAPABILITIES=compute,utility